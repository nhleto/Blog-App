<h1>All Tags</h1>

<ul id = 'Tags'>
  <%= @tag.each do |tag| %>
    <li>
    <%= link_to tag, tag_path(tag), class: 'Tag' %>
    </li>
  <% end %>
</ul>
<p><%= link_to '<<Back to Articles list', articles_path %></p>
